apiVersion: v1
kind: Service
metadata:
  name: calculator-api
  namespace: default
spec:
  selector:
    app: calculator-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer